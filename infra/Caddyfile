# HTTPS reverse proxy for the API. DOMAIN comes from the environment, e.g. 203-0-113-10.sslip.io (no domain purchase needed:
# sslip.io resolves any dashed IP to that IP, and Caddy gets a real Let's Encrypt certificate for it).
{$DOMAIN} {
	encode gzip
	reverse_proxy api:4000
}
